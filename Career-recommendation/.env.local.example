# Authentication
# To fix the "OAuth client was not found" error:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Go to "APIs & Services" > "Credentials" 
# 4. Click "Create Credentials" > "OAuth client ID"
# 5. Set Application Type to "Web application"
# 6. Add authorized redirect URI: http://localhost:3001/api/auth/callback/google
# 7. Copy the generated Client ID and Client Secret and paste them below
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Create a strong random string for the secret (you can use https://generate-secret.vercel.app/32)
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3001

# Email
# These settings need to be configured if you want to send emails
EMAIL_SERVER_HOST=smtp.example.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=username
EMAIL_SERVER_PASSWORD=password
EMAIL_SERVER_SECURE=false
EMAIL_FROM=noreply@example.com

# Base URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000 